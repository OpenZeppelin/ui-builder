// Placeholder
export function getSolanaExplorerAddressUrl(address: string, _chainId?: string): string | null {
  return `https://explorer.solana.com/address/${address}`;
}

// Placeholder
export function getSolanaExplorerTxUrl(txHash: string, _chainId?: string): string | null {
  return `https://explorer.solana.com/tx/${txHash}`;
}
