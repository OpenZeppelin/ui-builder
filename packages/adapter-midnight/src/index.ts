import MidnightAdapter from './adapter';

// Re-export the main adapter class
export { MidnightAdapter };

// Optionally re-export types if needed
// export * from './types'; // No types.ts in Midnight adapter yet
